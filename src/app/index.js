import {BrowserRouter as Router, Routes, Route } from "react-router-dom";
import { Canvas } from "./components";

function App() {
  return (
  <Canvas>
    <Router>
      <Routes>

      </Routes>
    </Router>
  </Canvas>
  );
}

export default App;
